Название проекта
(до 30 символов с пробелами)	*
Вы - компания?	
Название комании 
*
Ф.И.О. контактного лица	*
Контактный телефон	*
E-mail	*
Описание проекта* 


Вид результирующей продукции (или технологии) проекта* 
(до 200 символов с пробелами) 
200
Базовые функции (по возможности не более 5)* 
(до 200 символов с пробелами) 
200
Дополнительные функции 
(до 500 символов с пробелами) 
500
Потенциальные потребители* 
(до 500 символов с пробелами) 
500
Существующие аналоги (перечислите и укажите ваши основные отличия)* 
(до 1000 символов с пробелами) 
1000
Отрасль	 *
Направление	 *
Добавить новое направление
Стадия проекта	 *
Надо для завершения	 *
Укажите сумму 

Введите код с картинки	*


  
 
Ближайшие события
30апреля
Первый открытый конкурс гражданских инновационных проектов организаций оборонно-промышленного комплекса России
все события
Твиттер
@InKulibin : #inkulibin 10 типов людей, которые необходимы малому бизнесу. http://t.co/QuCgcOD7VX

@InKulibin : #inkulibin ЦИТАТНИК ИННОВАТОРА: "Лучшие годы вашей жизни – когда вы решаете, что ваши проблемы принадлежат вам..." http://t.co/cuKDrPRJgA

@InKulibin : #inkulibin Риск минус рост. http://t.co/RjV0PiRzFG

@InKulibin : #inkulibin Московская биржа запускает нанооблигации. http://t.co/32q6gUfrW5

@InKulibin : #inkulibin ЦИТАТНИК ИННОВАТОРА: Сначала тебя игнорируют. Затем над тобой смеются. Потом с тобой борются. А потом ты побеждаешь. - М. Ганди

@InKulibin : #inkulibin Старт-даун: 10 идей для стартапов, которые никогда не срабатывают. http://t.co/8TPGpRlZM0

Смотреть на Твиттере всю ленту


CREATE TABLE project_branch
(
  id bigserial NOT NULL,
  title character varying(75) NOT NULL,
  date_create date NOT NULL,
  CONSTRAINT project_branch_pkey PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE project_branch
  OWNER TO garage;
COMMENT ON TABLE project_branch
  IS 'Отрасли проектов';
  
CREATE TABLE project_direction
(
  id bigserial NOT NULL,
  title character varying(75) NOT NULL,
  branch_id bigint,
  date_create date NOT NULL,
  CONSTRAINT project_direction_pkey PRIMARY KEY (id),
  CONSTRAINT project_direction_branch_id_fkey FOREIGN KEY (branch_id)
      REFERENCES project_branch (id) MATCH SIMPLE
      ON UPDATE CASCADE ON DELETE SET NULL
)
WITH (
  OIDS=FALSE
);
ALTER TABLE project_direction
  OWNER TO garage;
COMMENT ON TABLE project_direction
  IS 'Направление проекта';


CREATE TABLE project_requirements
(
  id bigserial NOT NULL,
  title character varying(75) NOT NULL,
  date_create date NOT NULL,
  CONSTRAINT project_requirements_pkey PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE project_requirements
  OWNER TO garage;
COMMENT ON TABLE project_requirements
  IS 'Что требуется для завершения проекта';

CREATE TABLE project_stage
(
  id bigserial NOT NULL,
  title character varying(75) NOT NULL,
  date_create date NOT NULL,
  CONSTRAINT project_stage_pkey PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE project_stage
  OWNER TO garage;
COMMENT ON TABLE project_stage
  IS 'Стадия проекта';
  


